# =============================================================================
# VideoAI — Python dependencies (Python 3.11, Windows)
# =============================================================================
# IMPORTANT — install PyTorch (CPU build) FIRST, from the CPU wheel index, so
# pip does not pull the ~2.5 GB CUDA build. Embeddings run on CPU here to keep
# the 4 GB of VRAM free for ASR and the LLM.
#
#   python -m pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
#   python -m pip install -r requirements.txt
#
# The pinned versions below are what this MVP was built and tested against.
# =============================================================================

# --- Video download & audio ---------------------------------------------------
yt-dlp==2026.7.4
imageio-ffmpeg==0.6.0        # bundles a static ffmpeg binary (no system install)
av==18.1.0                   # PyAV: probing / decoding fallback

# --- ASR (speech-to-text) -----------------------------------------------------
faster-whisper==1.2.1
ctranslate2==4.8.1

# --- Embeddings ---------------------------------------------------------------
sentence-transformers==3.4.1
transformers==4.57.6
tokenizers==0.22.2
huggingface_hub==0.36.2
# torch is installed separately from the CPU index (see header). Pinned target:
# torch==2.13.0+cpu

# --- Vector math / storage ----------------------------------------------------
numpy==2.4.6
scikit-learn==1.9.0          # cosine_similarity helper (optional but handy)

# --- LLM client (Ollama over HTTP) --------------------------------------------
httpx==0.28.1

# --- UI -----------------------------------------------------------------------
streamlit==1.61.1

# --- Config -------------------------------------------------------------------
PyYAML==6.0.3
